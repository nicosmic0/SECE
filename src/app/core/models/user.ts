export class User {
    id: string;
    email: string;  // email
    password: string;
    name: string;  // name
    rol: string;  // rol: admin, secre, estu
    carrera: number;
    token: string;
}